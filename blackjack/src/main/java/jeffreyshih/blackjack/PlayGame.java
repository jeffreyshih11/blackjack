package jeffreyshih.blackjack;

public class PlayGame {

	
	/*
	 * create dealer
	 * create deck and shuffle (just add cards back and shuffle if playing again)
	 * prompt player to enter name and then create human player
	 * ask if ready
	 * deal cards
	 * player first, then dealer
	 * dealer always has to hit if total < 17
	 * end game if human busts
	 * end game if dealer value goes above human total or bust
	 * or tie
	 * ask to play again
	 * 
	 */
}
